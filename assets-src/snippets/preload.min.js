var o = (function(n, t, e) {
  var r = jQuery;
  function i(n) {
    var t = n + "=";
    var r = decodeURIComponent(e);
    var i = r.split(";");
    for (var o = 0; o < i.length; o++) {
      var c = i[o];
      while (c.charAt(0) == " ") {
        c = c.substring(1);
      }
      if (c.indexOf(t) == 0) {
        return c.substring(t.length, c.length);
      }
    }
    return "";
  }
  r.cachedScript = function(n, t) {
    t = r.extend(t || {}, { dataType: "script", cache: true, url: n });
    return r.ajax(t);
  };
  return {
    isDev: i("dev") === "1",
    loadJs: function(n) {
      if (typeof n === "string") n = [n];
      (function t() {
        if (n.length > 0) {
          r.cachedScript(n.shift());
          t();
        }
      })();
    }
  };
})(window, document, document.cookie);
console.log(o);
