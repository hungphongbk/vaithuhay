{% capture blog %}{% for article in blogs.all.articles limit: 32 %}{% include 'get-img-blog' %}{ "title": {{ article.title | json }},"author": {{ article.author | json }},"url": {{ article.url | json }},"created": {{ article.created_at | date: '%d/%m/%Y %H:%M:%S' | json }},"image": {{ imageUrl | json }},"excerpt": {{ article.content | strip_html | truncatewords: 20 | json }} }{% unless forloop.last %},{% endunless %}{% endfor %}{% endcapture %}{% capture top %}{% endcapture %}<div id="app"></div><script> var slideLinks=[{% for i in (1..4) %} {% capture link %}slide_{{ i }}_link{% endcapture %} {% capture	bgimage %}bgimage_{{i}}0.png{% endcapture %} {% capture show_slide %}slide_{{ i }}{% endcapture %} {{ settings[link] | json }},{{ bgimage | asset_url | json }},{{ settings[show_slide] }} {% unless forloop.last %},{% endunless %} {% endfor %}];var products_new = [{% for product in collections['san-pham-moi'].products %}{% include 'product-json.src' with product %},{% endfor %}];var products_sale ={% include 'product-tops' %};var blog = [{{ blog }}];vth.topProducts ={% include 'product-tops' %};</script>
